<?php
class StringHelperTest {
    public function run() {
        $tests = [
            "Nguyễn Văn A" => "Nguyen Van A",
            "Đặng Thùy Trâm" => "Dang Thuy Tram",
            "Lê Lợi" => "Le Loi",
            "Test No Accents" => "Test No Accents",
            "" => "",
            "123" => "123"
        ];
        
        $pass = 0;
        foreach ($tests as $input => $expected) {
            $actual = $this->removeAccents($input);
            if ($actual === $expected) {
                $pass++;
            } else {
                echo "FAIL: '$input' -> Expected '$expected', Got '$actual'\n";
            }
        }
        echo "Passed $pass / " . count($tests) . " tests.\n";
    }

    private function removeAccents($str) {
        $str = preg_replace("/(à|á|ạ|ả|ã|â|ầ|ấ|ậ|ẩ|ẫ|ă|ằ|ắ|ặ|ẳ|ẵ)/", "a", $str);
        $str = preg_replace("/(è|é|ẹ|ẻ|ẽ|ê|ề|ế|ệ|ể|ễ)/", "e", $str);
        $str = preg_replace("/(ì|í|ị|ỉ|ĩ)/", "i", $str);
        $str = preg_replace("/(ò|ó|ọ|ỏ|õ|ô|ồ|ố|ộ|ổ|ỗ|ơ|ờ|ớ|ợ|ở|ỡ)/", "o", $str);
        $str = preg_replace("/(ù|ú|ụ|ủ|ũ|ư|ừ|ứ|ự|ử|ữ)/", "u", $str);
        $str = preg_replace("/(ỳ|ý|ỵ|ỷ|ỹ)/", "y", $str);
        $str = preg_replace("/(đ)/", "d", $str);
        $str = preg_replace("/(À|Á|Ạ|Ả|Ã|Â|Ầ|Ấ|Ậ|Ẩ|Ẫ|Ă|Ằ|Ắ|Ặ|Ẳ|Ẵ)/", "A", $str);
        $str = preg_replace("/(È|É|Ẹ|Ẻ|Ẽ|Ê|Ề|Ế|Ệ|Ể|Ễ)/", "E", $str);
        $str = preg_replace("/(Ì|Í|Ị|Ỉ|Ĩ)/", "I", $str);
        $str = preg_replace("/(Ò|Ó|Ọ|Ỏ|Õ|Ô|Ồ|Ố|Ộ|Ổ|Ỗ|Ơ|Ờ|Ớ|Ợ|Ở|Ỡ)/", "O", $str);
        $str = preg_replace("/(Ù|Ú|Ụ|Ủ|Ũ|Ư|Ừ|Ứ|Ự|Ử|Ữ)/", "U", $str);
        $str = preg_replace("/(Ỳ|Ý|Ỵ|Ỷ|Ỹ)/", "Y", $str);
        $str = preg_replace("/(Đ)/", "D", $str);
        return $str;
    }
}

// Since we cannot run this in the environment easily without PHP CLI, 
// this file serves as documentation and a verification script for the user.
// However, the Logic in Android (Java) uses Normalizer, which is standard.
// The Logic in PHP (if implemented) would use similar regex or iconv.
?>